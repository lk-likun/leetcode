# leetcode submit region begin(Prohibit modification and deletion)
class Solution:
    def checkString(self, s: str) -> bool:
        # lastA = -1  # 'a' 最后一次出现的下标
        # firstB = len(s)  # 'b' 第一次出现的下标
        # for i in range(len(s)):
        #     if s[i] == 'a':
        #         lastA = max(lastA, i)
        #     else:
        #         firstB = min(firstB, i)
        # return lastA < firstB


# leetcode submit region end(Prohibit modification and deletion)
